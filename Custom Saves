* I propose a feature for creating custom filenames for the application instead of using the deafeat "Thing.png"

	

line(20, 20, 80, 80);
// Saves a TIFF file named "diagonal.tif"
save("diagonal.tif");
// Saves a TARGA file named "cross.tga"
line(80, 20, 20, 80);
save("cross.tga");
